{
  "name": "onboarding-archer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-squid": "concurrently --raw --kill-others \"cd backend && npm run start-squid\" \"node sleep.js 3000 && cd frontend && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
