{
  "name": "onboarding-archer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-squid": "concurrently --raw --kill-others \"cd backend && npm run start-squid\" \"cd frontend && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"./**/src/**/*.{ts,js,tsx,jsx,md,mdx}\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "prettier": "^3.1.0"
  }
}
